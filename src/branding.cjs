const PRODUCT_NAME = "SwingSync";
const PACKAGE_NAME = "swingsync";
const CLI_COMMAND = "swingsync";

const DEFAULT_CACHE_FILENAME =
  ".swingsync-cache.json";

const LEGACY_CACHE_FILENAMES = [
  ".bpm-cache.json",
];

module.exports = {
  PRODUCT_NAME,
  PACKAGE_NAME,
  CLI_COMMAND,
  DEFAULT_CACHE_FILENAME,
  LEGACY_CACHE_FILENAMES,
};
